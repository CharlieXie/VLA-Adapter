[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "pyhash"
version = "0.0.0"
description = "Minimal pyhash compatibility shim for CALVIN"
requires-python = ">=3.8"

[tool.setuptools]
py-modules = ["pyhash"]
